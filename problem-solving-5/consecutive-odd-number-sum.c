#include<stdio.h>
int main(){
  int n;
  scanf("%d", &n);
  int x,y;
    int count = 0;
  for (int i = 0; i < count; i++)
  {
        if (n % 2 != 0)
        {
            printf("%d\n", n);
            break;
        }
        n++;
  }
  
    return 0;
}